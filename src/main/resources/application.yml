spring:
  application:
    name: enzuim
  datasource:
    url: jdbc:sqlite:enzuim.db
    driver-class-name: org.sqlite.JDBC
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.community.dialect.SQLiteDialect